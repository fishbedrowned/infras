repositories {
    mavenCentral()
}

jar {
    baseName = 'infras_api'
    version = baseVersion
    archiveName = 'infras_admin.jar'
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
